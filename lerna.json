{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "cnpm",
  "command": {
    "publish": {
      "ignoreChanges": ["*.lock"],
      "allowBranch": "master",
      "message": "chore(release): publish %s"
    },
    "bootstrap": {
      "npmClientArgs": ["--no-package-lock"]
    }
  }
}
