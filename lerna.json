{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "cnpm",
  "command": {
    "publish": {
      "ignoreChanges": ["*.lock"]
    },
    "bootstrap": {
      "npmClientArgs": ["--no-package-lock"]
    }
  }
}
