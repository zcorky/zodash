// import { Context } from '@koex/core';
// import { getBody } from './get-body';
// import { getHeaders } from './get-headers';
// import { getMethod } from './get-method';
// import { getUrl } from './get-url';
// import { getRedirect } from './get-redirect';

// import { RequestOptions, Options } from '../core/interface';

// export function getRequestOptions(ctx: Context, options: Options): RequestOptions {
//   const url = getUrl(ctx, options);
//   const method = getMethod(ctx);
//   const headers = getHeaders(ctx, options);
//   const body = getBody(ctx);
//   const redirect = getRedirect(options.followRedirects);

//   return {
//     url,
//     method,
//     headers,
//     body,
//     redirect,
//   };
// }
